 // The "User" interface defines the structure of a user object containing information about the user's credentials,
// first name, last name, teacher ID, and other related details.
interface User
{
    email: String,
    hesh_pass: String,
    login: String,
    name: String,
    surname: String,
    teacher_code: String,
    __v: String,
    _id: String
}

export default User;